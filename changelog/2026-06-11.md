# Changelog — 2026-06-11

## Day 01 — InfraWatch Foundation
**MR:** [feat(day-01): InfraWatch foundation — backend, frontend, CI, ops (#1)](https://github.com/kekcoke/Infraspekt/pull/1) · merged `46541f7`

### Changed
- Monorepo scaffold: `backend/`, `frontend/`, `ops/`, `docs/`, `infrastructure/`
- Express backend with `/health`, `/api/infrastructure/status`, `/api/infrastructure/disk` endpoints
- Static HTML dashboard polling backend every 30s (status) / 60s (disk)
- Multi-stage Dockerfiles for backend (Node 18 Alpine, non-root) and frontend (nginx)
- `docker-compose.yml` with healthcheck gating frontend on backend readiness
- GitHub Actions CI: lint + test + docker build on push to main/develop/feature/**
- ESLint config enforced in CI (`backend/.eslintrc.json`)
- Day 01 runbook: deployment, rollback, and incident triage template

### Deferred
- `/api/infrastructure/status` returns hardcoded uptime strings — replace with real metrics when data layer introduced (est. Day 10–15)
- Branch protection rules not yet enforced on GitHub — add before Day 02

### Fixed
- None

---

## Day 02 — Python Flask Backend
**MR:** [feat(day-02): Python Flask backend — blueprints, metrics, 100% test coverage](https://github.com/kekcoke/Infraspekt/pull/4) · pending merge

### Changed
- New `python-backend/` service (Flask 2.3, Python 3.11, gunicorn) on port 5001
- App factory pattern + 3 Blueprints: `health`, `infrastructure`, `metrics`
- 5 endpoints: `/api/v1/health`, `/servers`, `/clusters`, `/metrics/response-times`, `/metrics/error-rates`
- Request-duration middleware: structured log + `X-Response-Time-Ms` header
- 15 tests (unit + integration), 100% coverage, flake8 clean
- `python-backend/Dockerfile` with non-root user and HEALTHCHECK
- `docker-compose.yml` updated with `python-backend` service
- CI: `lint-and-test-python` job added (Python 3.11, flake8, pytest `--cov-fail-under=80`)
- `docs/architecture/day_02_spec.md`, `ops/runbooks/day_02_runbook.md`

### Deferred
- Mock data layer in python-backend — replace with real metrics when DB introduced (est. Day 10–15)
- PR #4 merge pending (PAT lacks merge permission — merge manually)

### Fixed
- None
